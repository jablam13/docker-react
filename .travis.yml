sudo: required
services:
    - docker

before_install:
    - docker build -t jablam13/docker-react -f Dockerfile.dev .

script:
    # - docker run jablam13/docker-react npm run test -- --coverage
    - docker run -e CI=true jablam13/docker-react npm run test